import { MAXIMUM_PASSWORD_LENGTH } from './common';

export const REQUIRED_PASSWORD = 'Please input a password';
export const MAXIMUM_PASSWORD_MESSAGE = `Password is too long, maximum length is ${MAXIMUM_PASSWORD_LENGTH} characters`;
